# Правила для Cline

- Веди ченжлог: при каждом изменении функциональности и перед каждым релизом/тегом обязательно добавляй запись в секцию ченжлога `DEVELOPMENT.md` (что изменилось и почему), а тело GitHub-релиза делай из этих же пунктов.
