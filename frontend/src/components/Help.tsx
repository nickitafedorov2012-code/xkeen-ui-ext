import type { ReactNode } from 'react'
import type { StatusInfo } from '../types'

interface Props {
  status: StatusInfo | null
}

function H({ children }: { children: ReactNode }) {
  return <h3 style={{ margin: '16px 0 6px', fontSize: 14.5 }}>{children}</h3>
}

function P({ children }: { children: ReactNode }) {
  return <p style={{ margin: '4px 0', fontSize: 13.5, lineHeight: 1.55 }}>{children}</p>
}

function K({ children }: { children: ReactNode }) {
  return <code style={{ background: 'var(--panel-2)', borderRadius: 4, padding: '1px 5px', fontSize: 12.5 }}>{children}</code>
}

export default function Help({ status }: Props) {
  return (
    <div style={{ maxWidth: 900 }}>
      <section className="card">
        <h2>📖 Справка — все функции панели</h2>
        <P>
          XKeen Route {status ? `(${status.version})` : ''} — веб-панель для per-device split routing:
          выбираете, какие устройства ходят через прокси-серверы Mihomo (XKeen), а какие — напрямую.
          Конфиг панели: <K>/opt/etc/xkeen-route/config.json</K>.
        </P>
        <Part1 />
        <Part2 />
        <Part3 />
      </section>
    </div>
  )
}

function Part1() {
  return (
    <>
      <H>📊 Дашборд</H>
      <P><b>Роутер</b> — модель, прошивка, версия KeeneticOS (через RCI API). <b>Mihomo</b> — версия ядра и тип (meta). <b>Активный сервер</b> — текущий сервер прокси и пинг до него. <b>Failover</b> — состояние автопереключения. Данные обновляются автоматически каждые N секунд (настраивается).</P>
      <H>🛰 Серверы</H>
      <P><b>Карточки серверов</b> — все прокси из config.yaml (включая серверы из proxy-providers, 100+). Зелёная метка — активный. Пинг показывается после нажатия «Пинг».</P>
      <P><b>Смена сервера</b> — клик по карточке переключает группу PROXY на выбранный сервер (через Clash API), применяется мгновенно, без разрыва интернета.</P>
      <P><b>🔍 Пинг</b> — замер задержки до одного сервера или всех сразу (параллельно, таймаут 2 сек).</P>
      <P><b>🚫 Игнор-лист</b> — исключить серверы из автогрупп Fastest/Fallback (exclude-filter в proxy-providers). Применяется сразу. При очистке списка исходные фильтры провайдеров восстанавливаются.</P>
      <P><b>Починить имена</b> — ремонт «кракозябр» (mojibake) в названиях серверов от провайдера (битая кодировка UTF-8/CP1251) плюс удаление флагов-эмодзи из имён.</P>
      <H>📱 Устройства</H>
      <P><b>Таблица устройств</b> — все клиенты роутера (IP, MAC, имя, интерфейс, текущий сервер/политика).</P>
      <P><b>Политика доступа</b> — per-device маршрутизация: <K>default</K> (как все), <K>block</K> (запрет интернета), <K>custom</K> (своя группа). Меняется через RCI API Keenetic, применяется сразу.</P>
      <P><b>⚡ Цепочка серверов (per-device routing)</b> — для устройства задаётся основной сервер + резервы. Панель создаёт в config.yaml AUTO-группу вида <K>AUTO-ip</K> и назначает её устройству. Порог пинга 0 = наследовать глобальный.</P>
      <P><b>⚡ Failover устройств</b> — фоновая проверка цепочек каждые interval_secs: если основной сервер упал (пинг выше порога) — автоматическое переключение на резерв; при восстановлении — возврат (если включён auto-restore).</P>
    </>
  )
}

function Part2() {
  return (
    <>
      <H>⚙️ Настройки — Failover</H>
      <P><b>Включить автоматический failover</b> — мониторинг приоритетного сервера: упал — переключение на лучший доступный.</P>
      <P><b>Порог пинга, мс</b> — сервер считается упавшим, если пинг выше порога (по умолчанию 300).</P>
      <P><b>Приоритетный сервер</b> — куда возвращаться при восстановлении связи.</P>
      <P><b>Интервал проверки, сек</b> — как часто фоновый монитор выполняет проверку (минимум 15).</P>
      <P><b>🔍 Тестовая проверка сейчас</b> — разовая проверка без ожидания интервала.</P>
      <H>⚙️ Настройки — RCI (Keenetic)</H>
      <P>Подключение к API роутера: host/порт (по умолчанию 127.0.0.1:79), логин. <b>Токен</b> — X-Ndma-Tkn; если пусто, панель берёт его из /opt/etc/xkeen/xkeen.json, иначе использует challenge-auth с логином/паролем. Пароль — только fallback.</P>
      <H>⚙️ Настройки — Mihomo</H>
      <P>Clash API ядра: host/порт (127.0.0.1:9090), secret (если задан в config.yaml). <b>Путь к config.yaml</b> — файл, который правит панель (домены, per-device группы, игнор-лист). <b>Провайдеры групп устройств</b> — какие proxy-providers подключать к AUTO-группам устройств; пусто = все найденные в config.yaml автоматически.</P>
      <H>⚙️ Настройки — Панель</H>
      <P><b>Интервал автообновления</b> — период опроса статуса на дашборде (3–300 сек).</P>
      <P><b>Уровень логов</b> — info (всё), warn (предупреждения и ошибки), error (только ошибки).</P>
      <P><b>Логировать HTTP-запросы</b> — писать в журнал каждый запрос к API панели.</P>
      <P><b>Удалённый syslog</b> — адрес host:port (UDP, RFC 3164) для дублирования всех записей журнала на внешний сервер (rsyslog, Graylog, Loki и т.п.). Пусто = не отправлять. Вступает в силу после перезапуска панели: sh /opt/etc/init.d/S99xkeen-route restart.</P>
    </>
  )
}

function Part3() {
  return (
    <>
      <H>🖥 Сервис XKeen</H>
      <P>Управление сервисом XKeen: <b>Статус</b>, <b>Старт</b>, <b>Рестарт</b>, <b>Стоп</b> (через init-скрипт, путь настраивается). Внимание: рестарт XKeen перегенерирует config.yaml — правки панели (домены, per-device, игнор-лист) сбросятся к исходным.</P>
      <H>💾 Бэкапы</H>
      <P><b>Создать бэкап</b> — снимок config.yaml (Mihomo) + config.json (панель) в каталог backup_dir/xkeen-route/xr-дата. <b>Восстановить</b> — перезаписать текущие конфиги из снимка + reload Mihomo. Крестик — удалить снимок. Каталог бэкапов настраивается (по умолчанию /opt/backups).</P>
      <H>🌐 Домены</H>
      <P>Списки доменов, применяемые сразу при сохранении (вставляются в начало rules: в config.yaml):</P>
      <P><b>⏭ Напрямую</b> — домены всегда мимо прокси (правило DOMAIN-SUFFIX → DIRECT): локальные сервисы, банки, госсайты.</P>
      <P><b>🔒 Принудительно через прокси</b> — домены всегда через прокси (DOMAIN-SUFFIX → PROXY), даже если устройство на direct-политике.</P>
      <H>📄 Журнал (логи)</H>
      <P>Файл /opt/etc/xkeen-route/xkeen-route.log: все действия (смена сервера, бэкапы, домены, failover, HTTP-запросы, ошибки). Ротация при 2 МБ (старая копия — .log.old). <b>Обновить</b> — перечитать хвост, <b>Скачать</b> — весь файл, <b>Очистить</b> — стереть, <b>автообновление</b> — перечитывать каждые 5 сек.</P>
      <H>🧰 Установка и обновление</H>
      <P>Одной командой (Entware): curl -Ls https://cdn.jsdelivr.net/gh/nickitafedorov2012-code/xkeen-ui-ext@v1.0.0/setup.sh | sh — откроется меню (установить / обновить / удалить / выйти).</P>
      <P>Обновление — та же команда (конфиг сохраняется). Удаление: пункт 3 меню, либо sh … setup.sh -s -- uninstall (конфиг сохраняется) / uninstall purge (полностью).</P>
      <H>🔴 Живые логи и 📈 Sparkline</H>
      <P>В «Журнале» есть режим 🔴 live — WebSocket-поток новых строк в реальном времени (без опроса). На дашборде под активным сервером — sparkline стабильности пинга за последние ~40 опросов (провалы отмечены красным).</P>
      <H>🔌 API (для скриптов)</H>
      <P>GET /api/status — сводка; GET /api/servers — список серверов; POST /api/servers/switch (server_id) — смена; GET /api/logs?lines=N — журнал; GET /api/logs/download — файл лога. Формат ответа: {'{ success, data | error }'}.</P>
    </>
  )
}
